import React from "react";

const learning = () => {
    return (
        <div>learning</div>
    )
}

export default learning;

// rafce

// Arrow Function
// const App = () => <h1>Olá, Mundo!</h1>;


// Nomeação de Componente
// PascalCase

// Nomeação de Variável, Função...
// camelCase

// Nomeação de Classes
// kebab-case

// export default : posso importar com qulquer nome e sem chaves
// export "sem defaut" : só posso importar entre chaves e com o nome exportado

// Self closing tag
// <Header></Header> == <Header />

// Nomeação de classes em CSS
// Metodogia BEM
// Blocks
// Elements
// Modifiers
//    bloco__elemento--modificador
//    exemplo: header__link--small

// Nomes compostos são separados por hífen (-)

// Tag vazia em React se chama fracment (fragmento)

// Componentes recebem "props"

// {Array(items)
// .fill()
// .map((currentValue, index) => (
//     <SingleItem key={`${title}-${index}`} />
// ))}

// Spread Operator
// ...

// Quando componentes se re-renderizam?
// Uma das ocasiões é quando uma variável de estado usada por esse componente é atualizada

// Hook - useState

// cd back-end
// npm init -y
// npm i express
// node --watch ./api/server.js

// npm install mongodb
// node .api/connect.js

// npm i axios
// npm i cors